[pytest]
markers =
    dsl
    coredeps

asyncio_mode = auto
filterwarnings =
    ignore:Authenticated towards inferred project 'test':UserWarning
    ignore:When using `include_outside_points=True` with a finite `limit` you may:UserWarning

addopts = --color=yes
