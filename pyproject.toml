[tool.black]
line-length = 120
target_version = ['py38']
include = '\.py$'

[tool.isort]
line_length = 120
multi_line_output = 3
include_trailing_comma = true
skip_glob = '^((?!py$).)*$'    # isort all Python files
float_to_top = true
